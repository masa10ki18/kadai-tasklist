<h1 <span class= "glyphicon glyphicon-modal-window"></span>id: <%= @task.id %> のタスク詳細ページ</h1>

<table class="table table-boraderd">
  <tr>
    <th>ステータス</th>
    <td><%= @task.status %></td>
  </tr>
  <tr>
    <th>タスク</th>
    <td><%= @task.content %></td>
  </tr>
</table>
  
  <%= link_to '編集', edit_task_path(@task),class: "btn btn-primary" %>
  <%= link_to '削除', @task, method: :delete, data: { confirm: '本当に削除してよろしいですか？' },class: "btn btn-danger" %>